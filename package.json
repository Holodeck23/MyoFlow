{
  "name": "myoflow",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "test:run": "turbo test:run",
    "test:coverage": "turbo test:coverage",
    "test:watch": "turbo test:watch",
    "test:e2e": "turbo test:e2e",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prisma:migrate:dev": "pnpm --filter db prisma migrate dev",
    "prisma:generate": "pnpm --filter db prisma generate",
    "prisma:studio": "pnpm --filter db prisma studio",
    "prisma:seed": "pnpm --filter db seed",
    "docker:up": "docker compose -f infra/docker/docker-compose.yml up -d",
    "docker:down": "docker compose -f infra/docker/docker-compose.yml down",
    "db:test": "docker compose -f infra/docker/docker-compose.yml up -d && sleep 5 && export DATABASE_URL=postgresql://postgres:password@localhost:5432/myoflow && pnpm prisma:migrate:dev && pnpm prisma:seed",
    "setup": "pnpm install && pnpm docker:up && sleep 3 && pnpm prisma:migrate:dev && pnpm prisma:seed"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.56.0",
    "eslint-config-next": "14.2.13",
    "prettier": "^3.0.0",
    "turbo": "^1.10.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "dependencies": {
    "@types/bcryptjs": "^3.0.0",
    "bcryptjs": "^3.0.2"
  }
}